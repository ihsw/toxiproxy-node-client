/// <reference path="../typings/main.d.ts" />

export default class Proxy {
    name: string;
    listen: string;
    upstream: string;
    enabled: boolean;
    upstream_toxics: any;
    downstream_toxics: any;
}